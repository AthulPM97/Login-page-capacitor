import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.signin.app',
  appName: 'login app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
